apiVersion: v1
kind: Service
metadata:
  name: safegate
  namespace: safegate
  labels:
    app: safegate
spec:
  type: ClusterIP
  selector:
    app: safegate
  ports:
    - name: http
      port: 8443
      targetPort: 8443
      protocol: TCP
